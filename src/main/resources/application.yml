spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/desapega
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    format-sql: true
    properties:
      hibernate:
        use_sql_comments: true
